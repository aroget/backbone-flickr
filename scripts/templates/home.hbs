<ul class="home-list">
  {{#each collection.[0].photosets.photoset}}
    <li class="six-50 columns">
      <h2>
        <a href="#/series/{{id}}">
          {{ title._content }}
        </a>
      </h2>
    </li>
  {{/each}}

</ul>